#include<stdio.h>
#include<string.h>
int main(){
    
    //  char str[] = "Hello World";
    // // printf("%s",str);
    // puts(str);
    // puts("I am learning C");
    char  str[40];
    gets(str);
    puts(str);
    return 0;
}

